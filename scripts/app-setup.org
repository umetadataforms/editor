#+title: App Setup
#+property: header-args :results verbatim
#+property: header-args+ :dir ../ :eval yes

* React App

#+begin_src shell :dir ../
npx create-react-app frontend --template typescript
#+end_src

#+RESULTS:
:
: Creating a new React app in /home/aa/projects/dataforms/frontend.
:
: Installing packages. This might take a couple of minutes.
: Installing react, react-dom, and react-scripts with cra-template-typescript...
:
:
: added 1324 packages in 53s
:
: 271 packages are looking for funding
:   run `npm fund` for details
:
: Installing template dependencies using npm...
:
: added 21 packages, removed 1 package, and changed 2 packages in 3s
:
: 271 packages are looking for funding
:   run `npm fund` for details
:
: Your tsconfig.json has been populated with default values.
:
: Removing template package using npm...
:
:
: removed 1 package, and audited 1344 packages in 3s
:
: 271 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.
:
: Success! Created frontend at /home/aa/projects/dataforms/frontend
: Inside that directory, you can run several commands:
:
:   npm start
:     Starts the development server.
:
:   npm run build
:     Bundles the app into static files for production.
:
:   npm test
:     Starts the test runner.
:
:   npm run eject
:     Removes this tool and copies build dependencies, configuration files
:     and scripts into the app directory. If you do this, you can’t go back!
:
: We suggest that you begin by typing:
:
:   cd frontend
:   npm start
:
: Happy hacking!

#+begin_src shell
# npm install --save-dev @types/react
#+end_src

* React JSON Schema Forms (RJSF)

#+begin_src shell
npm install --save-exact \
    @rjsf/core@5.24.12 \
    @rjsf/utils@5.24.12 \
    @rjsf/validator-ajv8@5.24.12
#+end_src

#+RESULTS:
:
: added 17 packages, and audited 1361 packages in 3s
:
: 271 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

* Theme

#+begin_src shell
npm install --save-exact @rjsf/antd@5.24.12
#+end_src

#+RESULTS:
:
: added 73 packages, and audited 1434 packages in 10s
:
: 273 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

#+begin_src shell
npm install --save-exact \
    antd@5 \
    @ant-design/icons@5
#+end_src

#+RESULTS:
:
: up to date, audited 1434 packages in 3s
:
: 273 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

* TipTap Text Editor

#+begin_src shell
npm install \
    @tiptap/react \
    @tiptap/starter-kit
#+end_src

#+RESULTS:
:
: added 67 packages, and audited 1501 packages in 6s
:
: 302 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

#+begin_src shell
npm install \
    @tiptap/extension-heading \
    @tiptap/extension-underline \
    @tiptap/extension-code \
    @tiptap/extension-bullet-list \
    @tiptap/extension-list-item \
    @tiptap/extension-link \
    @tiptap/extension-table \
    @tiptap/extension-table-header \
    @tiptap/extension-table-row \
    @tiptap/extension-table-cell
#+end_src

#+RESULTS:
:
: added 4 packages, and audited 1505 packages in 3s
:
: 306 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

#+begin_src shell
npm install dompurify
#+end_src

#+RESULTS:
:
: added 1 package, and audited 1764 packages in 1s
:
: 336 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

* Miscellaneous

#+begin_src shell
npm install lodash
#+end_src

#+RESULTS:
:
: up to date, audited 1505 packages in 1s
:
: 306 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

#+begin_src shell
npm install --save-dev @types/lodash
#+end_src

#+RESULTS:
:
: up to date, audited 1767 packages in 3s
:
: 336 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

#+begin_src shell
npm install react-resizable
#+end_src

#+RESULTS:
:
: up to date, audited 1767 packages in 1s
:
: 336 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

#+begin_src shell
npm install --save-dev @types/react-resizable
#+end_src

#+RESULTS:
:
: added 1 package, and audited 1768 packages in 3s
:
: 336 packages are looking for funding
:   run `npm fund` for details
:
: 27 vulnerabilities (24 moderate, 3 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

* Electron

#+begin_src shell
npm install --save-dev \
    electron \
    electron-builder \
    concurrently \
    wait-on \
    cross-env
#+end_src

#+RESULTS:
:
: changed 1 package, and audited 1766 packages in 55s
:
: 336 packages are looking for funding
:   run `npm fund` for details
:
: 11 vulnerabilities (3 moderate, 8 high)
:
: To address issues that do not require attention, run:
:   npm audit fix
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

* De-Duplicate

#+begin_src shell
npm dedupe
#+end_src

#+RESULTS:
:
: added 1 package, removed 3 packages, changed 4 packages, and audited 1766 packages in 23s
:
: 336 packages are looking for funding
:   run `npm fund` for details
:
: 9 vulnerabilities (3 moderate, 6 high)
:
: To address all issues (including breaking changes), run:
:   npm audit fix --force
:
: Run `npm audit` for details.

* Run Development

#+begin_src shell
# npm start
#+end_src

#+begin_src shell
# npm run dev
#+end_src

* Build using Electron

npm run build
npx electron-builder --linux AppImage
